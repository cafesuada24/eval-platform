[project]
name = "ai-chat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "evalplatform-sdk",
]

[tool.uv.sources]
evalplatform-sdk = { workspace = true }
