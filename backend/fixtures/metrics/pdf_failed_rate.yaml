name: "pdf_failed_rate"
type: "primitive"
description: "Evaluates the failure rate of PDF parsing operation as a percentage or fraction."
scoring_scale:
  min: 0.0
  max: 1.0
  data_type: "float"
required_inputs:
  - "pdf_failed_rate"
formula: "pdf_failed_rate"
