name: answer_relevancy_rigorous
type: ai-judge
description: A highly precise, multi-call evaluation that extracts sentences, classifies their individual relevance to the user prompt, and computes the exact score mathematically.
evaluation_strategy: answer_relevancy_rigorous
model_configuration:
  provider: openai
  model: gpt-4o
  temperature: 0.0
required_inputs:
  - input_text
  - output_text
scoring_scale:
  min: 0.0
  max: 1.0
  data_type: float
