name: faithfulness_rigorous
type: ai-judge
description: A highly precise, multi-call evaluation that extracts individual claims, verifies them independently, and computes the exact score mathematically.
evaluation_strategy: faithfulness_rigorous
model_configuration:
  provider: openai
  model: gpt-4o
  temperature: 0.0
required_inputs:
  - retrieved_context
  - output_text
scoring_scale:
  min: 0.0
  max: 1.0
  data_type: float
